// import { shallowMount } from '@vue/test-utils';
import StationChooser from './station-chooser.vue';

describe('StationChooser', () => {
  test('should render received stations list', () => {
    expect(true).toBeTruthy();
  });

  test('should filter list when user is typing letters on keyboard', () => {
    expect(true).toBeTruthy();
  });

  test('should disable buttons for invalid characters for each matched station', () => {
    expect(true).toBeTruthy();
  });

  test('should show loader during loading stations list', () => {
    expect(true).toBeTruthy();
  });

  test('should show error toast when error occurred during loading stations list', () => {
    expect(true).toBeTruthy();
  });
});
