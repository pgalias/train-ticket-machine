name: Code Coverage

on:
  push:
    branches: [ main ]
    
jobs:
  codecov:
    runs-on: ubuntu-latest
    steps:
      - name: Install modules
        run: yarn
      - name: Run tests
        run: yarn test
      - name: Upload code coverage
        uses: codecov/codecov-action@v1
